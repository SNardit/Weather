<resources>


    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="width_height_parents">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    
    <style name="margin_start_end">
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
    </style>

    <style name="margin_start_end_top" parent="margin_start_end">
        <item name="android:layout_marginTop">16dp</item>
    </style>

    <style name="text" parent="margin_start_end_top">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/textCityColor</item>
    </style>

    <style name="margin_top_start_for_weather_fragment">
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginStart">40dp</item>
    </style>

    <style name="margin_top_start_for_weather_fragment_normal">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginStart">20dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
